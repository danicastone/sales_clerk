<%= form_for(@marketer) do |f| %>
  <% if @marketer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@marketer.errors.count, "error") %> prohibited this marketer from being saved:</h2>

      <ul>
      <% @marketer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_area :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
